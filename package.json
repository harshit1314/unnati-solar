{
  "name": "unnati-solar",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "@tailwindcss/typography": "^0.5.16",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.5.0",
    "react-router-dom": "^6.16.0",
    "react-scripts": "5.0.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.3"
  },
  "scripts": {
    // "start": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    // "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "start": "npm start",
    "build": "npm build",
    "test": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts test",
    "eject": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts eject",
    "vercel-build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build"
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}